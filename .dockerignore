# Exclude files and directories from the Docker build context.
# Keeping the context small speeds up builds and avoids leaking secrets.

.dockerignore

# Ignore git metadata but keep HEAD/refs so we can retrieve commit SHA if needed.
.git
!.git/HEAD
!.git/refs

# Development artifacts
/cover/
/doc/
/test/
/tmp/
.elixir_ls

# Mix build outputs
/_build/
/deps/
*.ez

# VM crash dumps
/erl_crash.dump

# Static assets built during release
/assets/node_modules/
/priv/static/assets/
/priv/static/cache_manifest.json
