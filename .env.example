# Environment variables used by docker-compose and the release runtime.
# Copy this file to `.env` and fill in the values before deploying.

PHX_HOST=chess.dimamik.com
PORT=4000
SECRET_KEY_BASE=replace_with_a_secure_secret
# Optional: uncomment to enable Erlang distribution clustering.
# DNS_CLUSTER_QUERY=
